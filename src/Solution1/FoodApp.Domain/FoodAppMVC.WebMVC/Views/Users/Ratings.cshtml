@using FoodApp.Domain.Entities
@model List<Rating>

@{
    ViewData["Title"] = "Мої оцінки";
}

<h2>Ваші оцінки до страв</h2>

@if (!Model.Any())
{
    <p>Ви ще не залишили жодної оцінки.</p>
}
else
{
    <ul>
        @foreach (var rating in Model)
        {
            <li>
                <strong>@rating.Dish?.Name</strong>: @rating.RatingValue з 5
            </li>
        }
    </ul>
}
